{
  "name": "raidsnitch",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "turbo": "turbo",
    "build": "turbo run build"
  },
  "dependencies": {
    "prettier": "^3.0.0",
    "turbo": "^1.10.7"
  }
}
